# 发现记录

## 本轮任务（套图账本化）关键发现
- 现有 `handleBatchSetSubmit` 通过在 `currentSession.messages` 追加 user/model 消息实现，导致聊天区被套图步骤污染。
- 现有存储层 `services/storage.ts` 仅持久化 `sessions/templates/models`，没有面向“任务账本”的结构，无法支撑套图生命周期与检索。
- 局部编辑当前主路径默认写聊天消息；若套图迁出聊天流，需要额外的“无聊天写入”分支来回填到槽位版本历史。
- 现有 `BatchSetModal` 已能表达规则输入，可复用作为“创建任务”入口，不必重做配置表单。
- 侧栏“历史”当前只展示聊天 session，缺少独立套图入口，需要新增视图切换而不是继续堆在聊天里。

## 本轮实现结论（套图账本化）
- 已新增批量账本模型：`BatchJob / BatchSlot / BatchVersion / BatchActionLog`，并持久化到 IndexedDB（`batch_jobs`）。
- `handleBatchSetSubmit` 已改为“写账本不写聊天消息”；套图生成在独立任务中顺序执行并记录槽位状态。
- 左侧新增“套图记录”入口；主区域支持 chat / batch 双视图切换，batch 视图展示任务、槽位、版本历史与操作。
- 槽位操作已闭环：单槽重跑、设主图、下载、局部编辑（遮罩）并回写版本历史，不再污染聊天区。
- 素材库计数与列表已纳入批量任务产出图片，避免“套图图像不可见”问题。

## 本轮任务（上线安全基线）关键发现（改造前）
- 当时版本已具备登录 UI 与会话 cookie，但 `/api` 仍可在未登录时被代理访问，属于“仅前端门禁”。
- 当时前端代码仍依赖 `VITE_AUTHORIZATION`（会进入浏览器构建产物），不满足上线安全要求。
- 报错 `http proxy error: /auth/session ECONNREFUSED` 的根因是开发态只启动了 Vite 或端口冲突；现已把 auth 默认端口迁到 `3101` 并将 `dev` 改为并发启动。
- 要满足 MVP 上线基线，需要将上游密钥完全下沉到服务端，并在 `/api` 层强制鉴权和限流保护。

## 本轮实现结论（已完成）
- 后端 `server/index.mjs` 已实现：
  - `/api` 前置 `requireAuth`，未登录返回 401；
  - 仅服务端注入 `UPSTREAM_AUTHORIZATION` 到上游请求头；
  - 登录接口加入基于 `IP + 用户名` 的窗口限流（默认 10 分钟 5 次，超限封禁 30 分钟）；
  - 登录成功/失败基础审计日志。
- 前端去敏：
  - `services/apiConfig.ts` 不再读取任何前端鉴权 env；
  - `services/openaiImages.ts` 不再强制客户端 Authorization（改为可选头）。
- 开发代理收口：
  - `vite.config.ts` 中 `/api` 与 `/auth` 均转发至 auth server（默认 `http://localhost:3101`）。
- 联调结果：
  - 未登录访问 `/api/v1/models` => 401（通过）；
  - 登录后访问 `/api/v1/models` 已能到达上游（上游当前返回 429 invalid token cooldown，与本地鉴权链路无关）；
  - 连续 6 次错误密码，第 6 次返回 429（限流通过）。

## 当前架构（初始）
- 左侧栏已有「设置」折叠组、素材库入口、接口配置。
- 主流程已支持：批量尺寸、取消生成、错误详情弹窗、素材库、遮罩编辑、对比。
- 仍缺：错误直达修复入口、参数chips/提示词预设、素材快速复用动作、模特管理（重命名/删除）、部分性能优化。

## 本轮实现结果
- 接口层：`/v1/images/generations` 增加“模型不支持”自动兜底（拉取 `/v1/models` 选可用备选模型），并回传 `model_used/endpoint_used` 供前端展示。
- 错误体验：新增错误分类与中文友好文案；输入区上方支持“打开接口设置 / 重试 / 查看详情”；错误详情新增“建议操作”。
- 左侧体验：创作区改为“核心参数常显 + 高级项折叠”，减少折叠层级；增加连续编辑开关；侧栏中段改为可滚动。
- 素材复用：素材详情新增“一键设为参考图”“回填提示词”。
- 版面与性能：主内容区域取消固定窄宽，右侧可用空间更大；图片启用 `loading="lazy"` + `decoding="async"`。
- 中文化：运行时核心交互与错误链路已统一中文表达。

## 最新补充（本次）
- 持久化：会话/模板/模特已升级为 `IndexedDB` 持久化，并保留 `localStorage` 回退；应用启动时自动做本地数据迁移。
- 左侧底部：移除“接口设置折叠面板”，改为“模型切换器 + 余额显示（自动刷新）”。
- 余额接口：实现多端点兼容解析（`/v1/dashboard/billing/credit_grants` 等）；不支持时优雅降级为“暂不可用”。
